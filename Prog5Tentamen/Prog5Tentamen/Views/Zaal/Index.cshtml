@model IEnumerable<Prog5Tentamen.Models.Zaal>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Zitplaatsen)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Naam)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Zitplaatsen)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Naam)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ZaalId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ZaalId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ZaalId })
        </td>
    </tr>
}

</table>
